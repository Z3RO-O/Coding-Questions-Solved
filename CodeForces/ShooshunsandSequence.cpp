#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int n, k, val, max = MAX_INT;
    cin >> n >> k;
    vector<int> v;
    for (int i = 0; i < n; i++)
    {
        cin >> val;
        v.emplace_back(val);
    }
    for (int i = 0; i < n; i++)
    {
        
    }
    
    return 0;
}